// import axios from 'axios';

export const getPokemons = (queryText) => {
    // try {
    //     const response = await axios.get(`https://pokeapi.co/api/v2/pokemon/${queryText}`);

    //     console.log(response);
    // } catch (error) {
    //     console.error(error);
    // }
}